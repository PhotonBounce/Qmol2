name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy to VPS (placeholder)
        run: |
          echo "Deploying to production VPS..."
          # ssh user@vps "cd /opt/compute-swarm && git pull && docker compose up -d --build"
          echo "TODO: Configure SSH key secret and deploy script"

      - name: Notify deployment (placeholder)
        run: |
          echo "Sending deployment notification..."
          # curl -X POST ${{ secrets.DEPLOY_WEBHOOK_URL }} -d '{"status":"ok"}'
          echo "TODO: Configure webhook or Slack notification"
