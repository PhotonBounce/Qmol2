# Q-Mol Core Requirements
# ======================
# Install with: pip install -r requirements.txt
# This is the MINIMUM set needed to run Q-Mol with SQLite (free tier / self-hosted).

# Cheminformatics & computation
rdkit>=2024.3.1
numpy>=1.26
scipy>=1.12
pandas>=2.2
pyarrow>=15.0

# HTTP API
fastapi>=0.110
uvicorn>=0.27
python-multipart>=0.0.20
email-validator>=2.0
bcrypt>=4.0

# Database (SQLite async + PostgreSQL driver, but PostgreSQL is optional)
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.20          # SQLite async driver — REQUIRED for SQLite mode

# ML inference
onnxruntime>=1.17
scikit-learn>=1.4

# Utilities
requests>=2.31
tqdm>=4.66
python-dotenv>=1.0

# --- Optional extras (install only if needed) ---
# PostgreSQL production: pip install -r requirements-postgres.txt
# Full stack:           pip install -r requirements-full.txt
# Lite (smallest):      pip install -r requirements-lite.txt
